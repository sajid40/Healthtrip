import React from 'react';

const Treatments = (props) => {
    return (
        <div className="px-5 py-4  text-center bg-gray-300 rounded-xl ">
         <h2> {props.Name}</h2> 
        </div>
    );
};

export default Treatments;